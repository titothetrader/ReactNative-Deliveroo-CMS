import { View, Text } from 'react-native'
import React from 'react'

// Empty header in order to bypass default Expo Router navigation header
const Header = () => {
  return (
    <Text/>
  )
}

export default Header